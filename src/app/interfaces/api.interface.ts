export interface getInfoInterface {
    virtualPort: string,
    atrributedValue: string
}

export interface updateInfoInterface {
    virtualPort: string,
    atrributedValue: string,
    updateStatus: string,
    updateStatusCode: number
}